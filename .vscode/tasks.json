{
    "version": "1.0.0",
    "options": {
        "env": {},
    },
    "tasks": [
        {
            "label": "complileC++",
            "command": "make",
            "type": "shell",
            "options": {
                "cwd": "${fileDirname}"
            },
            "group": "test",
            // "runOptions":  
        }

        {
            "label": "rootRun",
            "command": "root",
            "args":[
                "-l",
                "-q",
                "${fileBasename}"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "group": "test",
            "dependsOrder": "sequence",
            "dependsOn":[
                "complileC++",
            ],
        }

        
        
        {
            "label": "root run",
            "command": "/cvmfs/cms.cern.ch/slc7_amd64_gcc900/cms/cmssw/CMSSW_12_2_4/external/slc7_amd64_gcc900/bin/root",
			"args": [
				"-l",
				"-q",
				"${fileDirname}/${fileBasename}"
			],
        }
    ]
}