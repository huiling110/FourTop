# FourTop Analysis Configuration - 1tau1l Channel (New Format)
# Created: 2025-12-06
# Updated: 2025-12-08 (migrated to new minimal format)

metadata:
  name: "1tau1l_TTBBtest"
  description: "TTBB integration testing with ghost-matching overlap removal"
  created: "2025-12-06"

# Version strings for each pipeline stage
versions:
  stage1: "v94HadroPreJetVetoHemOnly_TTBBtest"  # Object selection output
  stage2: "v1baselineHadro"                      # Variable calculation output
  hist: "v0BDT1tau1l_TTBBtest"                   # Histogram version
  datacard: "v0_ttbb_nosmoothing"                        # Datacard version
  combination: "combinationV21"                  # Combine version

# Channel name (simple string, not dict)
channel: "1tau1l"

# Eras to process
eras:
  - "2018"
  - "2017"
  - "2016postVFP"
  - "2016preVFP"

# Analysis options (normalized key names)
options:
  fake_tau: true
  mc_fake_tau: false
  blind: false
  systematics: true  # Enabled for Phase 8 testing
  smoothing: false

# Base paths
paths:
  nanoaod_base: "/publicfs/cms/data/TopQuark/nanoAOD"
  output_base: "/publicfs/cms/user/huahuil/tauOfTTTT_NanoAOD/forMVA"
