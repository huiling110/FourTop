# FourTop Analysis Configuration - 1tau0l Channel
# TTBB Integration Testing
# Created: 2025-12-06

metadata:
  version: "v1_TTBBtest"
  created: "2025-12-06"
  channel: "1tau0l"
  description: "TTBB integration testing with ghost-matching overlap removal"

# Path components for building full paths
# Stage 1 output: /publicfs/cms/user/huahuil/tauOfTTTT_NanoAOD/UL{era}/{in_version}/mc/
# Stage 2 output: /publicfs/cms/user/huahuil/tauOfTTTT_NanoAOD/forMVA/{era}/{out_version}_{in_version}/mc/
# Stage 3 output: .../mc/variableHists_{hist_version}/
paths:
  base: "/publicfs/cms/user/huahuil/tauOfTTTT_NanoAOD/forMVA"
  out_version: "v1baselineHadro"
  in_version: "v94HadroPreJetVetoHemOnly_TTBBtest"
  hist_version: "v0BDT1tau0l_TTBBtest"
  datacard_version: "v1_TTBBtest"

# Channel configuration
channel:
  name: "1tau0l"
  regions:
    - "1tau0lSR"
    - "1tau0lCRMR"
    - "1tau0lVR"
  variables:
    - "BDT"

# Eras to process (testing 2018 first)
eras:
  - "2018"

# Analysis options
options:
  ifFakeTau: true
  ifMCFTau: false
  ifBlind: false
  ifVLL: false

# Stage 2 specific config
stage2:
  if1tau2l: 0  # 0=1tau0l/1tau1l, 1=1tau2l
  JESVariationType: 0
  JESVariation: 0

# Smoothing configuration (Stage 4.2.5)
smoothing:
  enabled: false

# Combine configuration (Stage 4.4, 4.5)
combination:
  version: "combinationV21"
  card_dir: "run2_1tau0l_TTBBtest"
