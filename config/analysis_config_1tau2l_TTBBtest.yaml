# FourTop Analysis Configuration - 1tau2l Channel (New Format)
# Created: 2025-12-11
# Based on: analysis_config_1tau1l_TTBBtest.yaml

metadata:
  name: "1tau2l_TTBBtest"
  description: "1tau2l channel testing - nominal workflow validation"
  created: "2025-12-11"

# Version strings for each pipeline stage
versions:
  stage1: "v94HadroPreJetVetoHemOnly_TTBBtest"  # Object selection output
  stage2: "v1baselineHadro"                      # Variable calculation output
  hist: "v0BDT1tau2l_TTBBtest"                   # Histogram version (channel-specific)
  datacard: "v0_ttbb_nosmoothing"                # Datacard version
  combination: "combinationV21"                  # Combine version

# Channel name (simple string, not dict)
channel: "1tau2l"

# Eras to process
eras:
  - "2018"
  - "2017"
  - "2016postVFP"
  - "2016preVFP"

# Analysis options (normalized key names)
options:
  fake_tau: true
  mc_fake_tau: false
  blind: false
  systematics: false  # Start with nominal only for 2018 testing
  smoothing: false

# Base paths
paths:
  nanoaod_base: "/publicfs/cms/data/TopQuark/nanoAOD"
  output_base: "/publicfs/cms/user/huahuil/tauOfTTTT_NanoAOD/forMVA"
